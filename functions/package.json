{
  "name": "axis-dashboard-functions",
  "private": true,
  "main": "lib/src/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "rimraf lib && tsc",
    "clean": "rimraf lib",
    "test": "rimraf lib/test && vitest run test",
    "bootstrap:sheets": "npm run build && node lib/scripts/bootstrap.js"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "googleapis": "^173.0.0",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.9.3",
    "vitest": "^4.0.17"
  }
}
